from distutils.core import setup

setup(name='pubplot',
	  version = '0.1',
	  description = 'Matplotlib setting and functions for publication',
	  author = 'Feng Wang',
	  packages = ['pubplot'],
	  package_dir = {'pubplot': './'})
